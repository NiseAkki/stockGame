const API_CONFIG = {
  baseURL: 'http://localhost:8080/api',
  endpoints: {
    register: '/register',
    login: '/login'
  },
  headers: {
    'Content-Type': 'application/json'
  }
};

export default API_CONFIG; 