{
  "name": "stockGame",
  "version": "1.0.0",
  "description": "多人在线炒股游戏",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd client && npm install && NODE_ENV=production npm run build && cp -r dist/* ../public/",
    "heroku-postbuild": "cd client && npm install && NODE_ENV=production npm run build && cp -r dist/* ../public/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "sequelize": "^6.37.1",
    "ws": "^8.13.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NiseAkki/stockGame.git"
  }
} 