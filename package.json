{
  "name": "stockGame",
  "version": "1.0.0",
  "description": "多人在线炒股游戏",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd client && CI=true npm install --include=dev && npm run build && mkdir -p ../public && cp -r build/* ../public/",
    "heroku-postbuild": "cd client && CI=true npm install --include=dev && npm run build && mkdir -p ../public && cp -r build/* ../public/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "sequelize": "^6.35.1",
    "uuid": "^9.0.1",
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/NiseAkki/stockGame.git"
  }
}
